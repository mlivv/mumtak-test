import { QuestionsFetch } from "./questionsFetch";

export async function GetQuestions(){
    const data = await fetch(QuestionsFetch);
    // const map = 
    // capire se fare la mappatura come nel progetto di asana
}